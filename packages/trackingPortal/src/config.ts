// types
import { DefaultConfigProps } from "@trackingPortal/types/config";

export const drawerWidth = 260;

const config: DefaultConfigProps = {
  defaultPath: "/",
  fontFamily: `'DM Sans', sans-serif`,
  i18n: "en",
  miniDrawer: false,
  container: true,
  mode: "light",
  presetColor: "theme4",
  themeDirection: "ltr",
};

export default config;
